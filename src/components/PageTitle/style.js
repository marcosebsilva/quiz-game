import styled from 'styled-components';

const StyledTitle = styled.h1`
    font-size: 5rem;
    font-weight: 250;
    position: absolute;
    top: 15vh;
    i {
      color: #FD28C1;
      font-style: normal;
      font-weight: 900;
    }
`;

export default StyledTitle;
