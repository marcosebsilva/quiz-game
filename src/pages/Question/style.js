import styled from 'styled-components';

const StyledOptionsWrapper = styled.div`
  display: grid;
  grid-template-columns: repeat(2, 300px);
  grid-template-rows: repeat(2, 70px);
  grid-row-gap: 50px;
  grid-column-gap: 100px;
`;

export default StyledOptionsWrapper;
